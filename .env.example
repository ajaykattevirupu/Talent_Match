# Copy this file to .env and fill in your values
# Then run: source .env  (or use python-dotenv)

OPENAI_API_KEY=sk-your-key-here

# Local PostgreSQL (Docker quickstart below)
DATABASE_URL=postgresql://postgres:password@localhost:5432/talent_match

# --- Docker quickstart (pgvector) ---
# docker run --name pgvector \
#   -e POSTGRES_PASSWORD=password \
#   -e POSTGRES_DB=talent_match \
#   -p 5432:5432 \
#   -d ankane/pgvector:latest
